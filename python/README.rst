MOIST Python API
================

Minimal Python interface for the GEMS solvation model in ``moist``.

This package provides access to the existing ``moist`` C API through CFFI.
The low-level bindings live in ``moist.library``.